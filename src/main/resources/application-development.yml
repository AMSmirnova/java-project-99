spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:project
    username:
    password:
  h2:
    console:
      enabled: true
      path: /h2-console/
      settings:
        web-allow-others: true
